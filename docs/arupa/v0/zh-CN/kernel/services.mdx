---
title: "服务"
description: "通过服务目录和 API 安装、管理 Arupa 服务。"
---

内核会从 `ServiceDir` 中发现服务。服务包是一个 `.plg` 文件。内核会扫描该目录，读取每个包的 `info.yaml`，并根据服务配置决定是否启动它。

## 安装服务

Arupa 不支持通过管理 API 上传服务包。要安装一个服务：

1. 将其 `.plg` 包放入已配置的 `ServiceDir` 中。
2. 确保内核对 `ServiceTempDir` 具有写入权限，内核在加载服务包时会使用该目录。
3. 调用 `GET /api/services/discovered` 扫描该目录，或者重启 Arupa，使其在启动期间扫描该目录。
4. 如果该服务生效的 `Restart` 设置未启用自动启动，请手动启动它。

默认目录为：

```toml
ServiceDir = "services"
ServiceTempDir = "tmp"
```

该包必须是一个有效的服务归档文件，且在根目录下带有 `info.yaml`。该元数据必须提供服务名称、版本、运行时类型（`wasm`、`grpc` 或 `static`）、契约版本，以及对于 `wasm` 和 `grpc` 服务而言的启动命令。扫描时会忽略非 `.plg` 包的文件。

关于自动启动、执行用户、访问组和服务参数，请参阅[服务配置](./config-services)。

## 管理 API

所有服务管理接口都位于 `/api/services` 下，且需要经过身份验证的用户。它们返回 Arupa 使用的标准 JSON 响应结构：`success`、`message` 和 `data`。

### 列出已发现的服务

```http
GET /api/services/discovered
```

该接口会在返回结果之前先扫描已配置的 `ServiceDir`，因此列表始终反映该目录当前的内容；不存在单独的扫描接口。响应是一个按服务名称索引的 `services` 列表，每个条目都把包元数据与生效配置、当前生命周期状态结合在一起。

默认情况下，每个条目都会省略两个可能体积较大的字段：包 `metadata` 和配置 `params`。可以通过 `include` 查询参数添加其中之一或两者，支持逗号分隔、重复参数，或两者混用：

```http
GET /api/services/discovered?include=metadata,params
GET /api/services/discovered?include=metadata&include=params
```

`metadata` 和 `params` 是仅有的两个受支持的取值，其他任何值都会导致 `400 Bad Request`。

例如，加上 `include=metadata` 后：

```json
{
  "success": true,
  "message": "Discovered services fetched",
  "data": {
    "services": [
      {
        "name": "login",
        "version": "0.2.0",
        "type": "static",
        "contract_version": 2,
        "command": "",
        "package_path": "services/login.plg",
        "config": {
          "restart": "always"
        },
        "metadata": {
          "Category": "core",
          "DisplayName": "Login Pages"
        },
        "status": "running"
      }
    ]
  }
}
```

这是一个 `static` 服务，所以 `command` 为空，没有任何东西需要运行。`config` 展示的是从 `[Services.default]` 和 `[Services.<name>]` 合并而来的生效配置——这里只有 `restart` 是非默认值，因此其他字段被省略。与 `config` 中的其他字段不同，`config.params` 即使在这个响应里也会被隐藏，除非加上 `include=params`：params 可能体积很大，也可能包含通过 `env://` 引入的密钥之类的值，因此该接口默认永远不会返回它。

### 列出正在运行的服务

```http
GET /api/services/running
```

与已发现列表不同，该接口不会扫描 `ServiceDir`——它直接返回内核的实时注册表，因此即使目录中当前存在从未启动过的包，它也只会展示实际正在运行的内容。该接口没有 `include` 参数：一个运行中的条目总是展示完整信息，包括它所注册的路由和传输。

```json
{
  "success": true,
  "message": "Running services fetched",
  "data": {
    "services": [
      {
        "instance_id": "login",
        "name": "login",
        "version": "0.2.0",
        "type": "static",
        "path": "services/login.plg",
        "transports": [
          {
            "id": "login-pages",
            "type": "static",
            "source": "build/"
          }
        ],
        "routes": [
          {
            "id": "login-pages",
            "transport": "login-pages",
            "http": {
              "pattern": "/pages/",
              "access": {}
            }
          }
        ]
      }
    ]
  }
}
```

条目按 `instance_id` 索引，与[路由与传输](../service/routing)中描述的是同一份投影。空的 `access: {}` 表示公开策略，参见[访问控制](./config-access)。

### 启动、停止或重启服务

这三个操作使用相同的请求体：

```http
POST /api/services/start
POST /api/services/stop
POST /api/services/restart
Content-Type: application/json

{"name":"hello"}
```

- `start` 按名称启动一个先前已扫描到的服务。
- `stop` 卸载一个正在运行的服务，并移除其所有存活的主机绑定，包括它注册的每一个传输和路由。
- `restart` 停止正在运行的实例，然后加载该服务名称对应的最新扫描包。

`name` 字段是必需的。这些操作不接受包路径；该服务必须已经出现在扫描结果中。每个操作的响应都会把该名称原样返回，例如：

```json
{
  "success": true,
  "message": "Service started",
  "data": {
    "name": "hello"
  }
}
```

`static` 服务在操作系统层面没有可以启动或停止的进程，但它仍会经历相同的生命周期：启动它会注册其声明的传输和路由，停止它会移除这些传输和路由。

### 读取服务目录配置

```http
GET /api/services/config
```

响应返回当前的 `service_dir` 和 `service_temp_dir`：

```json
{
  "success": true,
  "message": "Service config fetched",
  "data": {
    "service_dir": "services",
    "service_temp_dir": "tmp"
  }
}
```

### 更新服务目录配置

```http
PUT /api/services/config
Content-Type: application/json

{
  "service_dir": "services",
  "service_temp_dir": "tmp2"
}
```

两个字段都是必需的。该 API 会先创建对应目录，再保存配置、更新管理器，并扫描新的服务目录。响应会报告实际发生了哪些变化：

```json
{
  "success": true,
  "message": "Service config updated",
  "data": {
    "service_dir": "services",
    "service_temp_dir": "tmp2",
    "temp_dir_requires_restart": true,
    "temp_dir_restart_reason": "running services keep their current extraction directory",
    "discovered_service_count": 5,
    "scan_path_effective_immediately": true
  }
}
```

`scan_path_effective_immediately` 永远是 `true`：这个请求内部已经执行过一次扫描，用的就是新的 `service_dir`；`discovered_service_count` 就是那次扫描发现的包数量——所以这个响应告诉您的是这次重新扫描的实际结果，而不仅仅是"已安排一次扫描"。

`temp_dir_requires_restart` 反映的是一个真实的限制，而不是内核为了方便而设的提示：服务管理器只会在自己启动时读取一次 `ServiceTempDir`，所以在这里修改它，只会更新内核下次启动时会使用的值，而当前正在运行的内核仍会继续把包解压到旧目录，直到被重启为止。只有当您提交的 `service_temp_dir` 确实与之前的值不同时，该字段才为 `true`——例如，如果您再次发送同样的请求、但 `service_temp_dir` 没有变化，响应会是 `temp_dir_requires_restart: false`，因为没有任何东西需要重启才能生效。`service_dir` 没有对应的标志位，因为下一次扫描总会重新读取它，而这次请求本身就已经触发了一次扫描。`temp_dir_restart_reason` 是一段含义相同的人类可读字符串，方便直接在界面上展示，而不必把这段说明硬编码在别处。

## 身份验证与访问权限

本页的每个接口都使用内核的身份验证中间件。未经身份验证的请求会在处理程序运行之前被拒绝。您可以使用 `Route.Allow` 为这些路径添加组限制；请参阅[访问控制](./config-access)。
